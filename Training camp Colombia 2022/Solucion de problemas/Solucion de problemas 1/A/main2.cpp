#include <bits/stdc++.h>
using namespace std;

int main() {
    int t; cin >> t;
    for(int i=0; i<t ; i++) {

        int n; cin >> n;
        vector <string> names(n);
        vector <int> problems(n);
        vector <int> penalty(n);
        
        int max_value = 
    }
}
